getLine(xA, yA, xB, yB, null);

 // get the slope, the y intercept and angle (0-90 degrees) of the line: y=tan(angle)*(x-a)+b
 m= (yB-yA)/(xB-xA); 
 b= yA-(m*xA); 
 
//y= mx+b
//x= (y-b)/m
 //angle= atan(abs(m))*180/PI;

 x1 = (0-b)/m;
 y1 = 0;
 x2 = getWidth;
 y2 = m*x2+b;

makeLine(x1,y1,x2,y2);  
